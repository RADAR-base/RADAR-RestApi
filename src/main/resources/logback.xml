<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </layout>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <!--file>/usr/local/tomcat/logs/radar-restapi.log</file-->
        <file>/usr/share/tomcat8/logs/radar-restapi.log</file>

        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%date{yyyy-MM-dd HH:mm:ss,UTC} UTC [%thread] %-5level %logger - %msg%n</Pattern>
        </layout>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Docker -->
            <fileNamePattern>/usr/local/tomcat/logs/archived/radar-restapi.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Amazon -->
            <!--fileNamePattern>/usr/share/tomcat8/logs/archived/radar-restapi.%d{yyyy-MM-dd}.%i.log</fileNamePattern-->

            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>

    <root level="info">
        <appender-ref ref="FILE" />
    </root>
</configuration>